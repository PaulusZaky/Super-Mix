{% if sizes == blank %}
  {% assign sizes = '(min-width: 2000px) 1000px, (min-width: 1445px) calc(100vw / 2), (min-width: 1200px) calc(100vw / 1.75), (min-width: 750px) calc(100vw / 1.25), 100vw' %}
{% endif %}

<noscript aria-hidden="true">
  <img
    class="{{ class }}"
    src="{{ responsive-mobile-image | img_url: '550x' }}"
    {% if alt %}
      alt="{{ alt | escape }}"
    {% else %}
      alt="{{ responsive-mobile-image.alt | escape }}"
    {% endif %}
    {{ props }}
  />
</noscript>

<img
  loading="lazy"
  class="{{ class }}"
  {% if alt %}
    alt="{{ alt | escape }}"
  {% else %}
    alt="{{ responsive-mobile-image.alt | escape }}"
  {% endif %}
  {{ props }}
  width="{{ responsive-mobile-image.width }}"
  height="{{ responsive-mobile-image.height }}"
  srcset="{%- if responsive-mobile-image.width >= 550 -%}{{ responsive-mobile-image | img_url: '550x' }} 550w,{%- endif -%}
          {%- if responsive-mobile-image.width >= 1100 -%}{{ responsive-mobile-image | img_url: '1100x' }} 1100w,{%- endif -%}
          {%- if responsive-mobile-image.width >= 1445 -%}{{ responsive-mobile-image | img_url: '1445x' }} 1445w,{%- endif -%}
          {%- if responsive-mobile-image.width >= 1680 -%}{{ responsive-mobile-image | img_url: '1680x' }} 1680w,{%- endif -%}
          {%- if responsive-mobile-image.width >= 2048 -%}{{ responsive-mobile-image | img_url: '2048x' }} 2048w,{%- endif -%}
          {%- if responsive-mobile-image.width >= 2200 -%}{{ responsive-mobile-image | img_url: '2200x' }} 2200w,{%- endif -%}
          {%- if responsive-mobile-image.width >= 2890 -%}{{ responsive-mobile-image | img_url: '2890x' }} 2890w,{%- endif -%}
          {{ responsive-mobile-image | img_url: 'master' }} {{ responsive-mobile-image.width }}w"
  sizes="{{ sizes }}"
  src="{{ responsive-mobile-image | img_url: '1445x' }}"
/>